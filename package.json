{
  "name": "openstart",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "node --check app.js && node --check core.js && node --check data.js && node --check payments.js && node --test tests/*.test.mjs"
  }
}
